# Copy this file to `.streamlit/secrets.toml` (which is git-ignored) to enable
# the optional password gate locally, or set the same key under
# "App settings -> Secrets" on Streamlit Community Cloud.
#
# Leave it unset for a fully public app.
app_password = "change-me-to-a-strong-passphrase"
